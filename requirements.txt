# MABE Detector SIFT — unified requirements
# ==========================================
# Install from /opt/detector-sift/:
#   pip install -r requirements.txt
#
# Covers: detector core, MABE generator, MCP server, agentic layer

# Core detection — required for all modes
pyyaml>=6.0

# Graph / simulation (MABE generator)
networkx>=3.2

# Numeric computation (velocity model, trimmed statistics)
numpy>=1.26

# Environment variable loading (.env support)
python-dotenv>=1.0

# LLM narrative enhancement + MCP tool invocation
anthropic>=0.40

# MCP server (agentic layer)
mcp>=1.0